function isUserValid(boolean) {
    return boolean;
}
var answer = isUserValid(true) ? "Thou mayest come in" : "You shall not pass!";

function condition(boolean) {
    if(condition(true)) {
        return "Thou mayest come in";
    } else {
        return "You shall not pass!"
    }
}